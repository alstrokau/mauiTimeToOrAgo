<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="mauiTimeToOrAgo.MainPage">

    <Grid Padding="20" RowDefinitions="*, auto">
        <!-- Subtle modern gradient background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F6FBFF" Offset="0" />
                <GradientStop Color="#EAF4FF" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Centered time card -->
        <VerticalStackLayout Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center" Spacing="18">
            <Border Padding="28" BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" 
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="24" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
                    <Label x:Name="lbOutMain"
                           Text="--:--:--"
                           FontSize="96"
                           FontAttributes="Bold"
                           HorizontalTextAlignment="Center"
                           TextColor="{AppThemeBinding Light={StaticResource MidnightBlue}, Dark={StaticResource White}}" />

                    <Label x:Name="lbOutSecond"
                           Text="--:--:--"
                           FontSize="32"
                           HorizontalTextAlignment="Center"
                           TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource Gray200}}" />
                </VerticalStackLayout>
            </Border>

            <!-- Optional subtle helper text to make the UI friendlier -->
            <Label Text="Countdown updates every second" HorizontalTextAlignment="Center" FontSize="12" Opacity="0.6"/>
        </VerticalStackLayout>

        <!-- Controls card -->
        <VerticalStackLayout Grid.Row="1" Padding="0,18,0,0" Spacing="12">
            <Border Padding="12" BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}" 
                    Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="10">
                    <Label Text="Set target date/time" Margin="2" FontSize="16" HorizontalTextAlignment="Center"
                           TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource Gray200}}" />

                    <HorizontalStackLayout Spacing="12" HorizontalOptions="Center">
                        <DatePicker x:Name="dpTargetDate" DateSelected="dpTargetDate_DateSelected" HorizontalOptions="FillAndExpand" />
                        <TimePicker x:Name="tpTargetTime" Format="HH:mm" TimeSelected="tpTargetTime_TimeSelected" HorizontalOptions="FillAndExpand" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>

    </Grid>

</ContentPage>
